<?xml version="1.0" encoding="UTF-8"?>
<!-- Copyright 2026 - PrimateUY / License AGPL-3.0 or later -->
<!--
	Las entradas de menú de lo que todavía no llegó. Cada una abre la misma pantalla con un
	texto distinto: qué va a hacer y en qué bloque llega.

	El texto NO es de relleno. Es lo único que separa «el menú está roto» de «esto está
	planificado», y quien lo lea seis meses después tiene que poder decidir si le sirve
	esperar o si necesita otra cosa.

	OJO CON EL FORMATO DEL `context`. Va en UNA SOLA LÍNEA por valor, sin partir el
	literal en pedazos. El evaluador de expresiones del NAVEGADOR no soporta la
	concatenación implícita de literales adyacentes —`'a' 'b'`— que sí es válida en
	Python: el servidor guarda el texto sin mirarlo y la pantalla revienta recién al hacer
	clic, con un «Can not parse python expression». Pasó, y no lo vio ningún test porque
	el módulo carga perfecto.
-->
<odoo>

	<!-- El panel dejó de ser un casillero: existe desde V4 del tramo visual. Su acción
	     está en repo_panel_views.xml. Lo que sigue apagado —la tendencia de 8 corridas y
	     la meta configurable— vive DENTRO de la pantalla, que es donde el usuario lo
	     necesita ver. -->

	<record id="action_pendiente_promocion_modulos" model="ir.actions.act_window">
		<field name="name">Promoción de módulos</field>
		<field name="res_model">repo.coming.soon</field>
		<field name="view_mode">form</field>
		<field name="target">current</field>
		<field name="context">{'prm_titulo': 'Promoción de un módulo entre repositorios', 'prm_bloque': 'Bloque D · después de armar planes sin JSON', 'prm_que': 'Mover un módulo al repositorio general que corresponda y sacarlo de los particulares donde estaba. La limpieza son commits de borrado en repositorios de clientes, así que entra por el mismo embudo que toda escritura: plan, aprobación, aplicación, bitácora y reversión.\n\nEl inventario de módulos —que ya funciona— es lo que dice dónde está cada uno y si las copias divergieron. Con copias divergentes la promoción no se arma sola: alguien tiene que decidir qué versión gana.'}</field>
	</record>

	<record id="action_pendiente_promocion_ramas" model="ir.actions.act_window">
		<field name="name">Promoción entre ramas</field>
		<field name="res_model">repo.coming.soon</field>
		<field name="view_mode">form</field>
		<field name="target">current</field>
		<field name="context">{'prm_titulo': 'Promoción entre ramas', 'prm_bloque': 'F4 · Operación', 'prm_que': 'Mover código de staging a support y de ahí a producción, con las validaciones previas y el merge por API.\n\nNo confundir con la promoción de MÓDULOS, que es entre repositorios y está en la sección Módulos. Son cosas distintas y por eso tienen entradas distintas.'}</field>
	</record>

	<record id="action_pendiente_prs" model="ir.actions.act_window">
		<field name="name">Pull requests</field>
		<field name="res_model">repo.coming.soon</field>
		<field name="view_mode">form</field>
		<field name="target">current</field>
		<field name="context">{'prm_titulo': 'Pull requests desde Odoo', 'prm_bloque': 'F4 · Operación', 'prm_que': 'Ver las PRs pendientes, vincularlas a proyectos y tareas, y aprobar, pedir cambios o mergear desde acá.\n\nNecesita una pieza que hoy no existe en el módulo: autenticación de USUARIO contra GitHub. Sin eso una aprobación saldría a nombre de la aplicación y no de la persona, y no contaría como revisión requerida.'}</field>
	</record>

	<record id="action_pendiente_rama_tarea" model="ir.actions.act_window">
		<field name="name">Crear rama desde tarea</field>
		<field name="res_model">repo.coming.soon</field>
		<field name="view_mode">form</field>
		<field name="target">current</field>
		<field name="context">{'prm_titulo': 'Crear una rama desde una tarea', 'prm_bloque': 'F4 · Operación', 'prm_que': 'Desde una tarea de proyecto, crear la rama con el nombre que la convención pide y dejarla vinculada, para que después el mensaje de commit y la PR se puedan validar contra una tarea que existe de verdad.'}</field>
	</record>




	<record id="action_pendiente_forks" model="ir.actions.act_window">
		<field name="name">Forks y su upstream</field>
		<field name="res_model">repo.coming.soon</field>
		<field name="view_mode">form</field>
		<field name="target">current</field>
		<field name="context">{'prm_titulo': 'Forks, su atraso y sus parches', 'prm_bloque': 'Bloque C', 'prm_que': 'Cuánto atrás está cada fork de su upstream, qué parches propios tiene encima, y la sincronización que los reaplica sin forzar nada cuando hay conflicto.\n\nHoy los forks se relevan y se clasifican, pero su atraso no se mide y sus parches no se siguen.'}</field>
	</record>


</odoo>
